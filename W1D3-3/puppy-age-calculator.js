const calculateDogAge = function(puppyAge) {
  console.log(
    "your Dog is" + " " + puppyAge * 7 + " " + "years old in dog years"
  );
};

calculateDogAge(3);
calculateDogAge(5);
calculateDogAge(1);
